package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class DataReaderTest {

    // so this method requires user input, meaning a test won't work
    @Test
    void printQueryResults() {
    }
}